export const nodeTypes = {
  "add": {
    name: "adder",
    inputs: ["x", "y"],
    outputs: ["sum"]
  },
  "number": {
    name: "number",
    inputs: [],
    outputs: ["value"]
  }
}

export function evaluator(graph, triggers) {
  
}